// This file runs before every Cypress spec.
// No global setup (custom commands, cy.intercept stubs, etc.) is needed yet,
// but it's kept as the wired-up place to add that once auth flows are
// exercised end-to-end.
export {};
